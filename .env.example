# Secret used to authenticate the GET /admin/stats endpoint.
# Generate a long random string (e.g. `openssl rand -hex 32`) and set it
# in your local .env (which is gitignored) and in your Render dashboard
# under Environment. Rotate periodically.
ADMIN_SECRET_KEY=
